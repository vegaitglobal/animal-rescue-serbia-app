export const Constants = {
  tokenPersistanceKey: 'accessToken',
  baseUrl: 'https://api.ars.codeforacause.rs', //TODO: Fix react-native-config library since, currently, it's not fetching env file values consistently
};
